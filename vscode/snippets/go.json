{
	"snippets main": {
		"prefix": "main",
		"body":[
			"package main\n",
			"func main() {",
			"\t$2",
			"}\n"
		],
		"description": "snippets main"
	},
	"snippets signal": {
		"prefix": "signal",
		"body":[
			"ch := make(chan os.Signal, 1)",
			"signal.Notify(ch, os.Interrupt, syscall.SIGTERM)",
			"select {",
			"case <-ch:",
			"}\n"
		],
		"description": "snippets signal"
	},
	"snippets go select": {
		"prefix": "go select",
		"body":[
			"go func() {",
			"\tfor {",
			"\t\tselect {",
			"\t\tcase <-time.After(time.Second):",
			"\t\t}",
			"\t}",
			"}()"
		],
		"description": "snippets go select"
	}
}